query RaceList($first: Int, $after: String) {
	races(first: $first, after: $after) {
		edges {
			node {
				id
				name
				description
				thumbnailId
				imageIds
				updated
			}
		}
		pageInfo {
			hasNextPage
			endCursor
		}
		totalCount
	}
}
