query ItemList($first: Int, $after: String) {
	items(first: $first, after: $after) {
		edges {
			node {
				id
				name
				description
				thumbnailId
				imageIds
				updated
			}
		}
		pageInfo {
			hasNextPage
			endCursor
		}
		totalCount
	}
}
