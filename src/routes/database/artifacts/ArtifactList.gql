query ArtifactList($first: Int, $after: String) {
	artifacts(first: $first, after: $after) {
		edges {
			node {
				id
				name
				description
				thumbnailId
				imageIds
				updated
			}
		}
		pageInfo {
			hasNextPage
			endCursor
		}
		totalCount
	}
}
